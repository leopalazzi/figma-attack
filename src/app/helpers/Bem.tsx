/** @format */

import { setup } from "bem-cn";

const Bem = setup({
    el: "__", // Separateur d'élément
    mod: "--", // Separateur de  modificateur
    modValue: "-", // Separateur de valeur de modificateur
});
export default Bem;